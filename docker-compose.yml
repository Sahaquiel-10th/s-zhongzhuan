services:
  app:
    build: .
    env_file: .env
    environment:
      DATABASE_PATH: /app/data/super-relay.db
    ports:
      - "4173:4173"
    volumes:
      - super_relay_data:/app/data
    restart: unless-stopped

volumes:
  super_relay_data:
